name: Build and release our Minitwit services (automatically)

on:
  schedule:
  - cron: '27 17 * * 4'

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Echo
      run: echo "Hello, world!"


